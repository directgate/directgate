{
  "name": "directgate-manager",
  "version": "1.0.0",
  "description": "DirectGate Manager - minimal GUI to pair devices and control the DirectGate agent service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.3.5"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
